# Third-party notices

The `novascale-agent` release binary links permissively licensed Go packages.
Their license and copyright notices are collected under
`third_party_licenses/` and are included in every binary release archive.

The collection was generated for `./cmd/novascale-agent` with
`github.com/google/go-licenses/v2` version `v2.0.1`. Regenerate and review it
whenever the dependency graph changes.
